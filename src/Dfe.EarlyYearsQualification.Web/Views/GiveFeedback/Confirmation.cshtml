@model Dfe.EarlyYearsQualification.Web.Models.Content.FeedbackFormConfirmationPageModel

@{
    ViewData["Title"] = "Give Feedback Confirmation Page";
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full govuk-grid-column-two-thirds-from-desktop">
        <govuk-panel heading-level="1">
            <govuk-panel-title id="success-message">@Model.SuccessMessage</govuk-panel-title>
        </govuk-panel>
        <div class="govuk-body govuk-!-margin-top-8" id="main-confirmation-body">@Html.Raw(Model.Body)</div>
        @if (Model.ShowOptionalSection)
        {
            <h2 class="govuk-heading-m govuk-!-margin-top-2" id="optional-body-heading">@Model.OptionalEmailHeading</h2>
            <div class="govuk-body govuk-!-margin-bottom-6" id="optional-confirmation-body">@Html.Raw(Model.OptionalEmailBody)</div>
        }
        @{
            var link = Model.ReturnToHomepageLink;
            <div class="govuk-body">
                <a id="return-button" href="@link!.Href" class="govuk-link"
                   target=@(link.OpenInNewTab ? "_blank" : "")>@link.DisplayText</a>
            </div>
        }
    </div>
</div>